
@model Vendor.Models.ViewModels.UserClaimsViewModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.claims)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
          <form asp-controller="Roles" asp-action="Claims">
               <div asp-validation-summary="ModelOnly" class="text-danger"></div>
               <input hidden asp-for="@Model.RoleId" />
@*@foreach (var item in Model.claims) {
      <div class="m-2 form-check">
                    <input asp-for="@item.ClaimName" hidden />
                    <input asp-for="@item.isSelected" class="form-check-input" />
                    <label class="form-check-label" asp-for="@item.isSelected">
                        @item.ClaimName
                    </label>
      </div>
}*@
@for(int i = 0;i<@Model.claims.Count;i++){
    <div class="m-2 form-check">
                    <input asp-for="@Model.claims[i].ClaimName" hidden />
                    <input asp-for="@Model.claims[i].isSelected" class="form-check-input" />
                    <label class="form-check-label" asp-for="@Model.claims[i].isSelected">
                        @Model.claims[i].ClaimName
                    </label>
      </div>
}
  <div asp-validation-summary="All" class="text-danger"></div>
 <div class="form-group">
                <input type="submit" value="Update Claims" class="btn btn-primary" />
 </div>
 </form>
    </tbody>
</table>
